# SupplyGuard 项目详细计划

> 文档状态：生效  
> 版本：v1.0  
> 制定日期：2026-06-21  
> 项目根目录：`D:\code\codex\工作项目`

## 1. 项目定义

### 1.1 项目名称

**SupplyGuard：面向开源项目的软件供应链安全分析平台**

### 1.2 项目定位

SupplyGuard 接收源码目录、依赖清单或 SBOM，解析直接依赖与传递依赖，结合公开漏洞数据生成风险分析、依赖路径、升级建议和 CI 门禁结果。

本项目不依赖付费模型、付费数据、云服务器或 GPU。核心能力必须能够在普通个人电脑上离线或低成本运行。

### 1.3 项目目标

1. 构建可复用的多生态依赖解析和漏洞匹配引擎。
2. 使用图结构解释漏洞如何通过传递依赖进入项目。
3. 支持 CycloneDX SBOM 的导入和导出。
4. 结合严重程度、依赖深度、可修复性和公开风险信号生成可解释评分。
5. 提供 CLI、REST API 和 Web 控制台三种使用方式。
6. 支持 CI 中的增量扫描和风险门禁。
7. 使用公开数据、公开仓库和自建固定样本完成可重复评测。
8. 形成可用于简历、面试和项目演示的完整工程材料。

### 1.4 非目标

以下内容不属于第一版承诺：

- 不开发漏洞利用代码，不提供攻击自动化能力。
- 不声称能够证明项目“绝对安全”。
- 不在 MVP 中实现跨语言的完整调用图或漏洞代码级可达性证明。
- 不自动修改用户项目的依赖文件；第一版只生成建议和补丁预览。
- 不把大语言模型作为核心运行依赖。
- 不为了技术数量引入不必要的微服务、消息队列或 Kubernetes。
- 不依赖必须付费、必须绑定银行卡或试用期结束后失效的服务。

## 2. 用户与核心场景

### 2.1 目标用户

- 希望检查开源依赖风险的个人开发者。
- 需要在 CI 中增加供应链安全门禁的小型团队。
- 需要查看 SBOM、漏洞路径和修复建议的安全人员。

### 2.2 核心用户故事

1. 用户在仓库中执行 `supplyguard scan .`，得到可读报告和退出码。
2. 用户查看某个漏洞从直接依赖到问题包的完整路径。
3. 用户对比两个提交之间新增、修复和仍然存在的风险。
4. 用户导出 CycloneDX JSON SBOM 并用于其他安全工具。
5. CI 在新增高危漏洞且不满足豁免策略时阻止合并。
6. 用户查看建议升级哪个直接依赖可以用最小改动消除风险。
7. 完成首次漏洞数据同步后，用户可以在断网环境扫描本地项目。

## 3. 数据与零成本约束

### 3.1 允许的数据源

- OSV：漏洞、受影响版本和修复版本。
- deps.dev：软件包、版本和依赖关系的可选补充信息。
- OpenSSF Scorecard：开源仓库安全实践的可选风险信号。
- CycloneDX：SBOM 数据标准。
- SLSA：供应链安全设计参考。
- CISA KEV、EPSS 等可公开下载的数据：后续风险增强信号。
- 公开许可的开源仓库与自建测试夹具。

### 3.2 成本红线

- 项目额外金钱成本预算固定为 0 元；任何可选付费能力均不得进入默认开发、测试或演示链路。
- 项目运行不得要求任何付费 API。
- 项目测试不得要求云 GPU。
- 默认部署为本地运行，不要求购买服务器、域名或数据库服务。
- 可选在线增强功能必须能够关闭，关闭后核心扫描仍需正常工作。
- Codex、Claude Code 等开发工具不得成为产品运行时依赖。

## 4. 功能范围

### 4.1 MVP 必须完成

#### A. 依赖输入

- 扫描本地源码目录。
- 首批支持 `package-lock.json` 和 Python 的固定版本依赖输入。
- 导入 CycloneDX JSON SBOM。
- 对未知格式给出明确错误，不静默忽略。

#### B. 统一依赖模型

- 组件坐标：生态、名称、版本、命名空间。
- 直接依赖和传递依赖关系。
- 来源文件和证据位置。
- 依赖图去重、环检测和多路径保留。
- 使用稳定的内部组件 ID，保证重复扫描结果可比较。

#### C. 漏洞数据

- 下载并缓存 OSV 公开数据。
- 支持增量更新、更新时间记录和失败恢复。
- 建立本地查询索引。
- 根据生态、包名和版本匹配漏洞。
- 保存原始数据来源和更新时间，保证可追溯。

#### D. 分析结果

- 漏洞编号、摘要、严重程度和受影响范围。
- 漏洞对应的依赖节点。
- 从根项目到漏洞依赖的全部或截断后的代表路径。
- 是否存在已知修复版本。
- 基于规则的可解释风险分数。
- 扫描结果的 JSON 和终端报告。

#### E. SBOM

- 导出 CycloneDX JSON。
- 保留组件、版本、依赖边和必要元数据。
- 对导出的 SBOM 做 schema 或结构验证。

#### F. 质量保障

- 单元测试、集成测试和端到端测试。
- 固定漏洞样本和无漏洞对照样本。
- 解析异常、损坏数据、超时和断网场景测试。
- 所有扫描结果必须可复现。

### 4.2 v1.0 完整范围

- 增加 Maven/Gradle 中至少一种 Java 依赖输入。
- Web 控制台展示项目、扫描、漏洞、依赖图和历史趋势。
- REST API 支持提交扫描、查询进度和下载报告。
- 扫描任务持久化，进程重启后能够恢复或明确标记失败。
- 两次扫描之间的风险 Diff。
- 风险豁免策略，包括原因、责任人、失效时间和审计记录。
- CI 门禁，包括稳定退出码和机器可读报告。
- 最小升级建议与冲突说明。
- 导出 HTML 项目报告。
- 公开测试仓库清单、基准脚本和演示数据。

### 4.3 后续增强项

- 基于 import/调用信息的有限可达性分析，并明确精度边界。
- CISA KEV、EPSS、OpenSSF Scorecard 等风险信号。
- SPDX SBOM 支持。
- SARIF 输出及代码托管平台集成。
- 多项目资产清单和组织级策略。
- 插件式生态解析器。
- 签名、来源证明和 SLSA 相关检查。

## 5. 技术架构

### 5.1 默认技术栈

- 语言：Python 3.12+
- CLI：Typer
- API：FastAPI + Pydantic
- 数据访问：SQLAlchemy
- 默认数据库：SQLite
- 可选数据库：PostgreSQL
- 前端：React + TypeScript + Vite
- 测试：pytest + Hypothesis
- 质量检查：Ruff + mypy
- 本地编排：Docker Compose，仅作为可选方式
- 报告：JSON、CycloneDX JSON、HTML

选择 Python 是为了降低本地运行门槛、提高解析和数据处理效率。核心领域模型必须与 FastAPI 解耦，避免业务逻辑被 Web 框架绑定。

### 5.2 逻辑模块

```text
输入适配器
  ├─ package-lock
  ├─ Python 固定依赖
  └─ CycloneDX
        ↓
统一组件与依赖图
        ↓
漏洞数据仓库 ← OSV 同步器
        ↓
版本匹配与风险分析
        ↓
升级建议与策略门禁
        ↓
CLI / REST API / Web / CI 报告
```

### 5.3 预计目录结构

```text
工作项目/
├─ PROJECT_PLAN.md
├─ PROJECT_RULES.md
├─ REPORT.md
├─ README.md
├─ pyproject.toml
├─ supplyguard/
│  ├─ domain/
│  ├─ parsers/
│  ├─ graph/
│  ├─ advisories/
│  ├─ matching/
│  ├─ scoring/
│  ├─ remediation/
│  ├─ sbom/
│  ├─ storage/
│  ├─ api/
│  └─ cli/
├─ frontend/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  ├─ e2e/
│  └─ fixtures/
├─ benchmarks/
├─ docs/
├─ scripts/
├─ data/
└─ .tmp/
```

`data/` 中的大型公开数据缓存和 `.tmp/` 临时文件默认不得提交 Git。

## 6. 核心设计原则

### 6.1 正确性优先

- 不能解析的输入必须显式失败或产生警告。
- 漏洞匹配必须保存匹配依据。
- 缺少严重程度不得自动当作低风险。
- 任何推断都要与数据源事实区分。
- 升级建议必须说明是否经过真实依赖解析验证。

### 6.2 可解释性

每个风险结论至少包含：

- 数据来源。
- 匹配的软件包和版本。
- 受影响范围。
- 依赖引入路径。
- 评分组成。
- 可执行的下一步建议。

### 6.3 可重复性

- 固定输入和固定数据快照必须产生稳定输出。
- 报告记录工具版本、数据版本和扫描时间。
- 测试不得依赖随机外部状态；需要网络的测试必须单独标记。

### 6.4 安全性

- 将待扫描仓库视为不可信输入。
- 默认只解析文件，不执行项目脚本。
- 防止路径穿越、压缩炸弹、超大输入和恶意 JSON/XML。
- 不把令牌、密钥和本地绝对路径写入公开报告。
- 外部命令必须使用参数数组，避免 shell 拼接。

## 7. 风险评分初稿

风险评分使用明确规则而不是不可解释模型。初始维度：

| 维度 | 说明 |
|---|---|
| 严重程度 | CVSS 或来源提供的等级 |
| 依赖关系 | 直接依赖高于深层传递依赖 |
| 可修复性 | 有安全升级版本时提高处置优先级 |
| 风险证据 | KEV、EPSS 等公开信号，后续加入 |
| 使用证据 | 仅作为有限辅助信号，不冒充完整可达性证明 |
| 豁免状态 | 有期限、有理由的有效豁免可改变门禁但不删除事实 |

任何权重调整必须有测试样本和决策记录。

## 8. 里程碑与验收标准

### M0：治理和工程骨架

- 三份治理文档生效。
- 建立代码目录、测试目录和基础工具配置。
- 提供最小 CLI 和测试流水线。
- 所有路径写操作遵守 `PROJECT_RULES.md`。

### M1：依赖解析与统一图模型

- 完成两类 MVP 输入解析。
- 建立组件、边、来源证据和稳定 ID。
- 覆盖嵌套依赖、重复依赖、缺失版本和损坏文件。
- 依赖图能够输出代表路径。

### M2：OSV 数据同步与漏洞匹配

- 能够同步并索引所选生态的 OSV 数据。
- 支持离线查询。
- 固定已知漏洞样本得到预期匹配结果。
- 数据损坏或同步中断不会破坏上一份可用数据。

### M3：风险报告和 SBOM

- 生成终端、JSON 和 CycloneDX 报告。
- 展示依赖路径、风险评分和修复版本。
- 报告具有数据版本和证据字段。
- 完成报告结构测试和快照测试。

### M4：API 与 Web 控制台

- 创建并查询扫描任务。
- 展示项目概览、漏洞列表、依赖路径和历史 Diff。
- 所有错误状态能够在 UI 中解释。
- API 契约有自动化测试。

### M5：修复建议、策略与 CI

- 给出最小直接依赖升级建议。
- 支持风险阈值、豁免和过期检查。
- 提供适用于 CI 的稳定退出码。
- 完成新增漏洞阻断、已有漏洞不重复误报等场景测试。

### M6：基准、发布与简历材料

- 使用公开许可样本和自建夹具形成评测集。
- 提供性能、正确性和失败场景报告。
- README 包含架构、快速开始、演示和限制。
- 提供演示脚本、截图或录屏脚本。
- 提供简历描述和面试问答，但不虚构指标。

## 9. 质量指标

以下是验收目标，不是当前已实现指标：

- 核心领域与匹配模块测试覆盖率目标不低于 85%。
- 固定已知漏洞夹具中的预期匹配召回率为 100%。
- 相同输入、相同数据快照的结构化结果保持确定性。
- 暖缓存条件下，包含 1,000 个组件的依赖图扫描目标在普通开发机 5 秒内完成，不含首次数据下载。
- 无网络时，已同步数据上的扫描、报告和 SBOM 导出功能正常。
- 所有高风险文件解析入口具有大小限制和异常测试。
- 每个公开性能数字必须记录机器配置、数据规模和测量命令。

## 10. 测试策略

### 10.1 测试层级

- 单元测试：版本范围、组件 ID、解析器、图算法、评分规则。
- 属性测试：版本边界、顺序无关性、重复输入和异常结构。
- 集成测试：OSV 快照、数据库索引、SBOM 导入导出。
- 端到端测试：CLI 扫描到报告、API 扫描到结果。
- 安全测试：路径穿越、XXE、超大文件、恶意归档、命令注入。
- 回归测试：每次修复必须增加能够复现问题的测试。

### 10.2 测试数据原则

- 小型夹具直接保存在仓库。
- 大型公开数据只保存下载脚本、校验值和许可证说明。
- 外部仓库使用固定提交哈希，不依赖浮动分支。
- 不提交来源不明、许可不清晰或包含隐私的数据。

## 11. 风险登记

| 风险 | 影响 | 应对 |
|---|---|---|
| 不同包管理器语义复杂 | 解析结果错误 | MVP 限制生态，使用真实锁文件和对照工具验证 |
| OSV 数据量较大 | 下载和索引慢 | 按生态同步、增量更新、压缩缓存 |
| 升级建议产生冲突 | 建议不可执行 | 明确区分候选建议与已验证建议 |
| “可达性”被过度宣传 | 简历可信度降低 | 只陈述实现的静态证据和精度边界 |
| 前端消耗过多时间 | 核心能力被稀释 | M1-M3 完成后再开发 UI |
| 范围持续扩大 | 项目延期或跑偏 | 任何新增范围必须更新本计划和 REPORT |
| 外部服务变化 | 项目失效 | 本地缓存、适配器隔离、离线核心路径 |
| Codex 新线程缺乏上下文 | 重复或偏离计划 | 强制读取三份文档并持续更新 REPORT |

## 12. 交付物

- 可安装的 CLI。
- 可运行的本地 API 和 Web 控制台。
- 漏洞数据同步工具。
- CycloneDX SBOM 导入导出。
- CI 使用示例。
- 自动化测试和基准。
- 项目 README、架构文档、限制说明。
- 演示数据和演示脚本。
- 真实评测结果。
- 简历描述和面试讲解材料。

## 13. 完成定义

项目只有同时满足以下条件才可标记为 v1.0 完成：

1. M0-M6 的必须项全部验收。
2. 从干净环境可以按照 README 启动并运行演示。
3. 核心流程不需要付费服务和模型 API。
4. 自动化测试通过，已知限制被记录。
5. REPORT 中不存在未解释的高优先级阻塞项。
6. 所有展示指标均可通过仓库命令复现。
7. 文件边界和推进记录符合 `PROJECT_RULES.md`。

## 14. 变更控制

- 本计划是范围和验收的唯一基准。
- 小型实现调整记录到 `REPORT.md`。
- 改变目标、技术栈、MVP 范围或里程碑的决策，必须先修改本文件并在 REPORT 中说明原因和影响。
- 未经记录不得以“顺手优化”为理由扩大项目范围。
