# SupplyGuard 项目推进规则

> 文档状态：强制执行  
> 版本：v1.0  
> 生效日期：2026-06-21  
> 唯一可写项目根目录：`D:\code\codex\工作项目`

## 1. 规则优先级

1. 用户的当前明确指令优先于项目计划。
2. 本文件是所有 Codex 线程执行 SupplyGuard 工作时的强制规则。
3. `PROJECT_PLAN.md` 定义目标、范围和验收标准。
4. `REPORT.md` 定义当前事实、历史记录和下一步。
5. 若三者冲突，必须停止有冲突的修改，先在 REPORT 中记录并请求用户决定。

## 2. 文件系统边界：绝对硬规则

### 2.1 唯一可写范围

Codex 只允许在以下目录及其真实子目录中创建、修改、覆盖、移动或删除文件：

```text
D:\code\codex\工作项目
```

这里的“写入”包括但不限于：

- 创建、修改、覆盖或删除文件。
- 创建、修改或删除目录。
- 重命名或移动文件与目录。
- 生成构建产物、缓存、日志、数据库和临时文件。
- 修改 Git 元数据、钩子、配置或工作树。
- 创建虚拟环境、安装依赖或格式化代码。
- 通过脚本、安装器、测试、容器或子进程间接产生文件。

### 2.2 项目目录外权限

对于项目目录之外的任何路径，Codex 只有以下权限：

- 读取文件或目录信息。
- 将外部文件复制到项目目录内部。

严格禁止：

- 修改、覆盖、重命名、移动或删除外部文件。
- 在外部目录创建文件、目录、缓存、日志或临时文件。
- 修改用户级或系统级配置、环境、注册表、计划任务或服务。
- 全局安装软件包或修改全局包缓存配置。
- 修改其他仓库、工作树或 `.git` 目录。
- 将项目内文件移动到外部目录。

外部文件如需改变，Codex 只能给出操作说明，由用户在项目任务之外自行处理。该限制没有“为了方便”的例外。

### 2.3 复制规则

- 允许的复制方向只有：`外部只读来源 → 项目目录内部目标`。
- 复制前必须确认来源许可、目标位置和必要性。
- 不得使用移动代替复制。
- 不得复制密钥、令牌、浏览器数据、私人文档或来源不明的数据。
- 复制后的文件成为项目文件，后续修改仍需遵守本规则和许可证。

### 2.4 路径验证

每次执行写入、删除、移动、格式化、安装或可能产生文件的命令之前，必须：

1. 将目标路径转换成规范化绝对路径。
2. 确认目标等于项目根目录，或以项目根目录加路径分隔符开头。
3. 检查符号链接、目录联接和挂载点的真实目标；真实目标位于外部时禁止写入。
4. 对不存在的目标，根据其最近存在父目录验证边界。
5. 删除操作必须使用明确目标，不得对计算结果不明的路径执行递归删除。

路径字符串仅仅“看起来以项目路径开头”不够，例如 `工作项目-old` 不是项目子目录。

### 2.5 临时文件和缓存

- 所有临时文件必须写入 `D:\code\codex\工作项目\.tmp`。
- Python 虚拟环境必须位于项目内，例如 `.venv`。
- 下载缓存和公开数据必须位于项目内，例如 `data/cache`。
- 测试、编译和日志目录必须显式配置在项目内。
- 禁止依赖系统临时目录承载项目写入；若工具不可配置且必然写出项目目录，停止使用该工具。

### 2.6 Git 和依赖管理

- 仅当 `.git` 位于项目根目录内部时，才允许执行会修改 Git 状态的命令。
- 禁止修改全局 Git 配置、全局 hooks 或其他仓库。
- 禁止全局安装 Python、Node.js 或系统软件包。
- 依赖必须安装到项目内隔离环境，或仅使用已存在的只读工具。
- 包管理器缓存若无法重定向到项目内，不得执行安装命令。

### 2.7 容器和子进程

- 容器只允许挂载项目目录内部路径为可写卷。
- 外部路径不得以可写方式挂载。
- 子进程、测试和生成器同样受本文件边界约束。
- 启动服务前要明确其数据库、日志、缓存和上传目录全部位于项目内。

## 3. 每个 Codex 线程的强制启动流程

任何新线程在修改项目前必须依次完成：

1. 确认当前工作目录为 `D:\code\codex\工作项目`。
2. 完整阅读 `PROJECT_RULES.md`。
3. 阅读 `PROJECT_PLAN.md` 中当前里程碑、范围和验收标准。
4. 完整阅读 `REPORT.md` 的“当前状态”和最近三条推进记录。
5. 检查当前文件树和 Git 状态；不得覆盖无法解释的用户改动。
6. 在 REPORT 的当前会话中登记目标，或在结束时补全完整会话记录。
7. 只选择一个可验证的最小任务开始推进。

未完成上述流程不得开始代码修改。

## 4. 推进单位与报告频率

### 4.1 推进单位

以下任一情况均视为一个必须记录的“推进单元”：

- 一个 Codex 线程完成或暂停。
- 一个明确任务、缺陷或子功能完成。
- 一个里程碑状态发生变化。
- 技术方案、范围、依赖或数据源发生重要决策。
- 出现阻塞、失败、回滚或计划偏差。
- 即将进行上下文切换或将工作交给新线程。

### 4.2 强制更新 REPORT 的时机

- 修改代码或文档后、线程结束前。
- 完成测试或得到新的评测结果后。
- 发现重要风险或未知问题时立即记录。
- 修改 `PROJECT_PLAN.md` 时同步记录。
- 即使本次没有产生代码，也要记录调查结果、未修改原因和下一步。

任何推进单元都不得以“改动很小”为理由跳过报告。

## 5. REPORT 更新规范

每次推进必须同时维护：

1. `REPORT.md` 顶部的“当前状态快照”。
2. 文件底部的“推进记录”，按时间追加新条目。

推进记录必须包含：

- 时间和时区。
- 本次目标。
- 对应计划里程碑。
- 开始前状态。
- 实际完成内容。
- 创建、修改、删除的文件列表。
- 执行的关键命令和测试。
- 测试结果及可复现证据。
- 技术决策和理由。
- 未完成事项、风险和阻塞。
- 下一线程可以直接执行的具体步骤。
- 文件边界合规声明。

禁止：

- 删除历史推进记录。
- 修改历史事实以掩盖失败。
- 使用“已完成”“效果很好”等没有证据的描述。
- 把计划目标写成已经测得的结果。
- 只记录代码变化而不记录测试情况。

发现历史记录错误时，应追加勘误，不覆盖原记录。

## 6. 任务执行规则

### 6.1 小步推进

- 每次只处理边界清晰、可以验证的任务。
- 先写验收条件，再修改代码。
- 优先完成核心扫描链路，前端和装饰性工作不得抢占核心里程碑。
- 不进行与当前任务无关的大规模重构。

### 6.2 修改前检查

- 搜索相关实现、测试和文档。
- 检查文件是否包含用户未提交改动。
- 明确本次允许修改的文件集合。
- 确认不会产生项目目录外写入。

### 6.3 修改后验证

- 运行与改动直接相关的最小测试。
- 风险较高时运行完整测试、静态检查和安全测试。
- 检查新增文件是否意外包含密钥、绝对路径或大型数据。
- 检查实际 diff，确认没有无关变化。
- 将真实结果写入 REPORT。

### 6.4 缺陷处理

- 修复缺陷前先建立最小复现。
- 缺陷修复必须增加回归测试，无法增加时在 REPORT 解释原因。
- 不得通过跳过测试、吞掉异常或降低校验标准伪装修复。

## 7. 范围与架构控制

- 当前工作必须能够映射到 `PROJECT_PLAN.md` 的目标或里程碑。
- 新增重大功能前必须说明用户价值、成本和对当前里程碑的影响。
- 改变核心技术栈、数据模型或 MVP 范围前，先更新计划并记录决策。
- 禁止为了使用热门技术而增加没有必要的服务。
- 禁止将付费 API、云服务或闭源数据变成核心依赖。
- 可选增强不得破坏离线核心扫描能力。

## 8. 数据、隐私和安全规则

- 只使用许可证明确的公开数据、自建夹具或用户明确提供的数据。
- 大型数据只保存下载方式、校验值和许可证，除非确有必要纳入仓库。
- 不执行被扫描项目中的构建脚本、安装脚本或任意代码。
- 不读取与任务无关的用户隐私文件。
- 不将环境变量、令牌、用户名、绝对私人路径写入报告或测试快照。
- 日志默认脱敏，错误信息不得泄露密钥和完整外部路径。

## 9. 成本控制规则

- 默认额外金钱成本为 0 元。
- 不得调用任何会产生费用的 API。
- 不得创建付费云资源、订阅、电话线路、域名或托管数据库。
- 使用可能产生费用的服务前必须停止，由用户另行决定；该服务不得成为必需依赖。
- 优先使用本地计算、公开数据和可缓存的下载资源。

## 10. 完成与交接规则

一个任务只有满足以下条件才能标记完成：

1. 验收条件已经满足。
2. 相关测试已运行并记录真实结果。
3. 文档与实现一致。
4. REPORT 已更新。
5. 没有未说明的项目目录外写入。
6. 下一步和剩余风险清晰。

线程结束时必须保证新线程只阅读三份根文档和相关代码即可继续，不依赖上一线程的隐含记忆。

## 11. 违规处理

若发现准备执行或已经发生违反本规则的行为：

1. 立即停止相关操作。
2. 不得通过继续修改来掩盖问题。
3. 在 `REPORT.md` 记录目标路径、操作、原因和影响。
4. 仅在项目目录内进行安全检查和补救。
5. 外部目录若已被影响，只报告事实并由用户决定后续处理。

## 12. 新线程启动检查清单

```text
[ ] 当前目录是 D:\code\codex\工作项目
[ ] 已完整阅读 PROJECT_RULES.md
[ ] 已确认 PROJECT_PLAN.md 当前里程碑
[ ] 已阅读 REPORT 当前状态和最近三条记录
[ ] 已检查文件树和 Git 状态
[ ] 已明确本次任务与验收条件
[ ] 所有写入、缓存、日志和临时路径均在项目根目录内
[ ] 本次工作结束前会更新 REPORT.md
```

